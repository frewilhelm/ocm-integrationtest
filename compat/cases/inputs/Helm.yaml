# input:helm: local helm chart; both legs supported.
components:
- name: ocm.software/compat/input/helm
  version: 1.0.0
  provider:
    name: ocm.software
  labels:
  - name: compat.ocm.software/case
    value:
      id: input:helm
  - name: compat.ocm.software/fixtures
    value:
    - name: chart
      kind: writeChart
      with:
        path: chart
        name: compat
        version: 1.0.0
  - name: compat.ocm.software/expect
    value:
      construct:
        v1: pass
        v2: pass
      transfer:
        v1: pass
        v2: pass
  resources:
  - name: r-helm
    type: helmChart
    version: 1.0.0
    relation: local
    input:
      type: helm
      path: ${chart.path}
